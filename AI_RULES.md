# AI DEVELOPMENT RULES — قوانین اجباری

> **READ THIS FIRST.** If you are an AI agent working on this repository,
> you must read this file before making any change, and you must produce
> the final report in section 9 before ending your turn. These rules
> override any instinct to be encouraging.

این پروژه با کمک AI توسعه داده می‌شود. هدف این قوانین جلوگیری از
**تغییرات ناقص** و **ادعای اشتباه دربارهٔ وضعیت** است.

مالک پروژه لزوماً کد را خط‌به‌خط نمی‌خواند. بنابراین گزارش شما عملاً
تنها چیزی است که او می‌بیند. یک گزارش خوش‌بینانه بدتر از هیچ گزارشی است.

---

## ۰. واقعیت‌های محیطی این پروژه

قبل از هر ادعایی این‌ها را بدانید:

| واقعیت | پیامد |
|---|---|
| کد **Windows-only** است (WinDivert) | روی Linux فقط ماژول‌های منطق خالص کامپایل می‌شوند |
| `engine.rs` و `singleton.rs` تنها جاهای `unsafe` هستند | تغییر در آن‌ها ریسک بالاتری دارد |
| WFP callout driver **خارج از scope** است | `dns_guard` هرگز کامل نخواهد شد |
| بسیاری از محیط‌های sandbox به `crates.io` دسترسی ندارند | ممکن است اصلاً نتوانید `cargo` را اجرا کنید |

**اگر نتوانستید `cargo test` را اجرا کنید، این را با صدای بلند بگویید.**
ننویسید «تست‌ها پاس شدند». بنویسید `NOT TESTED` و دلیلش را ذکر کنید.

---

## ۱. قانون اصلی

هرگز فقط یک فایل را بر اساس درخواست کاربر تغییر ندهید.

قبل از هر تغییر:

1. ساختار پروژه را بررسی کنید (`TEST_MATRIX.md` و `ARCHITECTURE.md`).
2. قابلیت موردنظر را پیدا کنید.
3. تمام فایل‌ها و ماژول‌های وابسته را پیدا کنید.
4. مسیر اجرا را از ورودی تا خروجی دنبال کنید.
5. configuration، UI، backend، tests و documentation مرتبط را بررسی کنید.
6. مشکلات مرتبطی را که در همان محدوده پیدا می‌کنید فهرست کنید.

### زنجیرهٔ وابستگی این پروژه

```text
Configuration → Settings/Validation → Main → Engine → Pipeline
                                                          ↓
                                    Parsing → Processing → Packet handling

Web UI → Config API → Settings → Hot Reload → Runtime components

Strategy → Runtime decisions → Pipeline
```

`pipeline` به **۱۹ ماژول** وابسته است و `error` را **۲۹ ماژول** استفاده
می‌کنند. یک تغییر کوچک در آن‌ها اثر گسترده دارد.

---

## ۲. قبل از کدنویسی — گزارش کوتاه بدهید

### Current implementation
* قابلیت الان کجا پیاده شده؟ چه فایل‌هایی درگیرند؟
* چه قسمت‌هایی واقعاً کار می‌کنند؟ چه قسمت‌هایی ناقص‌اند؟

### Dependencies
* این قابلیت به چه ماژول‌هایی وابسته است؟
* چه چیزهایی به این قسمت وابسته‌اند؟
* آیا تغییر ممکن است جای دیگری را خراب کند؟

### Tests
* چه تستی وجود دارد؟ چه تستی وجود ندارد؟
* کدام قسمت فقط unit test دارد و نیاز به integration test دارد؟

---

## ۳. وضعیت هر Feature — فقط یکی از این‌ها

### `DONE`
فقط وقتی مجاز است که **همهٔ** این‌ها برقرار باشد:
- implementation کامل باشد
- مسیر اجرای واقعی وجود داشته باشد (تابع از کد تولیدی صدا زده شود، نه فقط از تست)
- هیچ stub یا placeholder مربوط به آن feature نباشد
- تست مناسب وجود داشته باشد
- **تست‌ها واقعاً اجرا شده باشند**
- dependencyها بررسی شده باشند

### `PARTIAL`
قسمتی کار می‌کند ولی محدودیت دارد. **محدودیت باید دقیقاً نوشته شود.**

### `STUB`
کد placeholder، fake، mock، `todo!()`، `unimplemented!()`، یا تابعی که
همیشه `Err` برمی‌گرداند.

### `UNTESTED`
implementation هست ولی تست مناسب اجرا نشده.

### `BLOCKED`
تکمیلش به dependency یا محیطی نیاز دارد که در دسترس نیست.

> **هرگز `STUB`، `PARTIAL`، `UNTESTED` یا `BLOCKED` را `DONE` اعلام نکنید.**

### تلهٔ خاص این پروژه

یک تابع می‌تواند پیاده‌سازی کامل و تست سبز داشته باشد و **باز هم**
`DONE` نباشد — اگر هیچ‌جای مسیر اجرای واقعی صدایش نزند.

`TEST_MATRIX.md` ستون `test-only` دارد که دقیقاً همین را نشان می‌دهد.
هر تابعی که آنجاست حداکثر `PARTIAL` است.

---

## ۴. هنگام Fix کردن

وقتی کاربر می‌گوید «فلان چیز را fix کن»، فقط اولین خطا را اصلاح نکنید:

1. root cause را پیدا کنید.
2. تمام نقاط affected را پیدا کنید.
3. تغییر را در همهٔ نقاط انجام دهید.
4. configuration و UI مرتبط را بررسی کنید.
5. tests را اضافه یا اصلاح کنید.
6. regression را بررسی کنید.

**اما** مشکلات کاملاً نامرتبط را بدون اجازهٔ کاربر تغییر ندهید — آن‌ها را
فهرست کنید و بپرسید.

### ترجیح راه‌حل ساختاری بر وصلهٔ نقطه‌ای

اگر یک مسیر خروجی فراموش شده، به‌جای اضافه‌کردن یک خط به همان مسیر،
از الگویی استفاده کنید که فراموش‌کردن را **غیرممکن** کند (مثلاً RAII
به‌جای فراخوانی دستی cleanup در هر `return`).

---

## ۵. بعد از تغییر

1. `cargo fmt --check`
2. `cargo clippy -- -D warnings`
3. `cargo test`
4. `cd uitest && npm test`
5. `python3 tools/gen_status.py` و commit کردن `TEST_MATRIX.md`

اگر هرکدام قابل اجرا نبود، بنویسید:

```
NOT TESTED — دلیل: ...
```

> **هرگز نتیجهٔ تست را حدس نزنید.** اگر `cargo` ندارید، ننویسید
> «باید کار کند». بنویسید که اجرا نشد.

---

## ۶. قانون "Done"

هرگز صرفاً به این دلایل نگویید feature کامل است:
* فایل ساخته شده
* تابع وجود دارد
* compiler error ندارد
* unit test ساده پاس شده
* UI دکمه دارد

Feature فقط وقتی `DONE` است که **رفتار مورد انتظار واقعاً verify شده باشد.**

---

## ۷. قانون Stub

هرگز یک Stub را با تغییر نام، wrapper، error handling یا comment
«تبدیل‌شده به implementation» فرض نکنید.

اگر implementation واقعی وجود ندارد، `STUB` باقی می‌ماند.

مثال واقعی از همین پروژه: `stealth::prevent_dns_leak` فقط یک
`pub use` از `dns_guard::block_port_53_except_localhost` است که همیشه
`Err` برمی‌گرداند. اسم قشنگ دارد، ولی `STUB` است.

---

## ۸. قانون Documentation

بعد از هر تغییر مهم، status پروژه را با واقعیت کد هماهنگ کنید.

**Documentation نباید از code جلوتر باشد.**

* `TEST_MATRIX.md` **تولید خودکار** است — دستی ویرایشش نکنید،
  `python3 tools/gen_status.py` را اجرا کنید.
* `STATUS.md` تنها منبع حقیقت دربارهٔ وضعیت است.
* اگر documentation می‌گوید `DONE` ولی code/test ثابتش نمی‌کند،
  **documentation را اصلاح کنید**، نه اینکه نادیده بگیرید.

---

## ۹. گزارش نهایی اجباری

در پایان هر task این ساختار را بدهید:

```markdown
### Changed
- فایل‌های تغییرکرده + خلاصهٔ تغییر هر کدام

### Verified
- چه دستوری اجرا شد و دقیقاً چه خروجی داد

### Not verified
- چه چیزی تست نشد و چرا

### Remaining
- مشکلات باقی‌مانده، Stubها، محدودیت‌ها

### Regression
- چه قسمت‌هایی ممکن است متأثر شده باشند
- چه تستی برایشان اجرا شد

### Final status
DONE | PARTIAL | STUB | UNTESTED | BLOCKED
```

> **هرگز بدون مدرک از کلمهٔ `DONE` استفاده نکنید.**

---

## ۱۰. مهم‌ترین قانون

اگر بین «چیزی که کاربر می‌خواهد بشنود» و «چیزی که واقعاً از کد و تست‌ها
ثابت شده» اختلاف وجود دارد، **همیشه حقیقت فنی را گزارش کنید.**

این پروژه به گزارش صادقانه نیاز دارد، نه گزارش امیدوارکننده.

### نمونهٔ نقض این قانون (اتفاق واقعی در این مخزن)

یک agent گزارش داد **«۳۶۹ تست پاس شد»** بعد از اینکه پنج باگ Rust را
رفع کرده بود. آن ۳۶۹ تست همگی **JavaScript** بودند و حتی یکی از آن‌ها
کد Rust تغییریافته را اجرا نمی‌کرد. عدد درست بود؛ جمله گمراه‌کننده بود.

شکل درستش:

> ۳۶۹ تست JS پاس شد. تست‌های Rust **اجرا نشدند** چون `cargo` در
> این محیط نصب نیست — رفع‌های Rust **کامپایل هم نشده‌اند**.

---

## چک‌لیست سریع پیش از پایان turn

- [ ] `TEST_MATRIX.md` را با `tools/gen_status.py` بازتولید کردم؟
- [ ] برای هر ادعا مدرک دارم (خروجی دستور، نه حافظه)؟
- [ ] چیزی را که اجرا نشده `DONE` نگفتم؟
- [ ] توابعی که فقط تست صدایشان می‌زند را `DONE` نگفتم؟
- [ ] گزارش بخش ۹ را نوشتم؟
- [ ] `KNOWN_ISSUES.md` را به‌روز کردم؟
